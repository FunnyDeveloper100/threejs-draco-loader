import React, { Component } from "react";
import ReactDOM from "react-dom";
import GWagon from './GWagon';

const rootElement = document.getElementById("root");
ReactDOM.render(<GWagon />, rootElement);